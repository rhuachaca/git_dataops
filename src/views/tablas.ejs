<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/static/tablas.css">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.16.0/umd/popper.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.0/js/bootstrap.min.js"></script>
	
    <title>Document</title>
</head>
<body>
    <div class="navbar navbar-expand-sm bg-dark navbar-dark">
		<a class="navbar-brand"><img src="/img/logo.jpeg" width="80" height="40"></a>
		<ul class="navbar-nav">
			<li class="nav-item">
				<a class="nav-link text-white" href="/comprobantes">Inicio</a>
			</li>
			<li class="nav-item">
				<a class="nav-link text-white" href="/registrar">Registrar Comprobante</a>
			</li>
			<li class="nav-item">
				<a class="nav-link text-white" href="/reportes">Reportes</a>
			</li>
            <li class="nav-item">
				<a class="nav-link text-white" onclick="window.print()" href="#">Imprimir</a>
			</li>
            <li class="nav-item">
				<a class="nav-link text-white" href="/">Cerrar Sesion</a>
			</li>
		</ul>
	</div>

    <table class="tabla">
        <thead>
            <tr>
                <th>Fecha Emision</th>
                <th>Fecha Vencimiento</th>
                <th><%= serie[0] %></th>
                <th>Cliente</th>
                <th>Tipo de Moneda</th>
                <th>Importe Total</th>
            </tr>
        </thead>
        <% if(typeof datos !== 'undefined'){ %>
            <% cont = 1 %>
            <% for (index in datos){ %>
                <% var cadena = datos[index].fecha_emi %>
                <% cadena = cadena.substr(0,7) %>
                <% if( (fecha  === cadena) || (fecha === '') ) { %>
                    <% if( ( tipomon === datos[index].Moneda) || ( tipomon === '')  ) { %>
                        <tr>
                            <td><%= datos[index].fecha_emi %></td>
                            <td><%= datos[index].fecha_venc %></td>
                            <td><%= serie[1] %>-<%= cont %></td>
                            <td><%= datos[index].Ruc %></td>
                            <td><%= datos[index].Moneda %></td>
                            <td><%= datos[index].Total %></td>
                        </tr>
                    <% } %>
                <% } %>
                <% cont = cont + 1 %>
            <% } %> 
        <% } %>
    </table>
</body>
</html>